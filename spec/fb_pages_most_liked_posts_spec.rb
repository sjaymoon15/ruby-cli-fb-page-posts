RSpec.describe FbPagesMostLikedPosts do
  it "has a version number" do
    expect(FbPagesMostLikedPosts::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
